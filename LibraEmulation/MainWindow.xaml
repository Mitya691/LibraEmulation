<Window x:Class="LibraEmulation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LibraEmulation.View"
        Title="Эмулятор Весов Tenso-M" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <local:LibraControl ScaleModel="{Binding LibraOne}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <local:LibraControl ScaleModel="{Binding LibraTwo}"/>
            </StackPanel>
        </Grid>
        <!-- Настройка соединения -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" >
            <local:ConnectionControl Width="768"/>
        </StackPanel>
        <!-- Лог обмена -->
        <TextBox Grid.Row="3" Text="{Binding LogText}" IsReadOnly="True" 
                 VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Margin="0,0,0,0"/>

    </Grid>
</Window>
